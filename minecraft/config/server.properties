# CLOUDFLARE_LOW_RESOURCE preset — conservative STARTING values.
# These are NOT benchmarked. Profile on real hardware before tuning.
motd=Minecraft God Server
server-port=25565
max-players=20
view-distance=6
simulation-distance=4
online-mode=true
enforce-secure-profile=false
sync-chunk-writes=false
network-compression-threshold=256
spawn-protection=0
allow-nether=true
generate-structures=false
level-type=minecraft\:flat
max-world-size=8000
entity-broadcast-range-percentage=60
max-tick-time=60000
